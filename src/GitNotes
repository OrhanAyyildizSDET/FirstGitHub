Always first pull  and then push!!!!
After first 1)git add .(All) or you can describe the spesific path with git.status 2)git commit -m "" 3)git push
==================
For formed new branch type "git branch (type branch name)"
For switch between type "git checkout (type branch name)"
For push codes in new formed branch type "git push --hatırlayamadım ..."