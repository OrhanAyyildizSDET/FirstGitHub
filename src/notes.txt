Always first pull  and then push!!!!
After first 1)git add .(All) or you can describe the spesific path with git.status 2)git commit -m "" 3)git push
=============
Branch for see have many branches type "git branch" or "git branch -a"
To Formed branch "git branch (type name)"
To checkout (to go to branch which we want) type "git checkout (branch name)"